{
  "buildCommand": "cd ../.. && pnpm install --no-frozen-lockfile && pnpm -F @ggai/user-app build",
  "outputDirectory": "dist",
  "installCommand": "",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
